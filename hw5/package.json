{
  "name": "hw5",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd frontend && yarn install && yarn add axios && yarn start",
    "server": "cd backend && yarn add cors express nodemon && yarn add -D @babel/cli @babel/core @babel/node @babel/preset-envyarn server"
  },
  "dependencies": {
    "axios": "^1.1.3"
  }
}
